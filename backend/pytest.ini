[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    audit: external audit tests (network, model downloads, paid APIs)
    slow: heavyweight tests
    integration: tests requiring external services/models
